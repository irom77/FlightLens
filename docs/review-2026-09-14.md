# FlightLens review handoff — 2026-09-14

Whole-repository review at `v0.10.1` (`main`, clean tree at review time).
Follow-up status is recorded below; the original findings describe that baseline.
Completed findings are implemented and tested, included in release **0.10.2**.
Platform validation still outstanding is tracked in [TODO.md](../TODO.md).

Completed as of 2026-09-16: **A1–A6**, **B1–B4**, **C1–C3**, **D1–D4**,
**E1**, and **E2**. No implementation findings remain open.
Packaged Windows/macOS validation remains outstanding for D4 and E1; the new CI
configuration still needs its first GitHub run.

Dismissed after verification on 2026-09-15: **E3** (already handled at the review
baseline by the theme store).

## What was checked

| Check | Result |
| --- | --- |
| `./test.sh` | passed end to end, exit 0 |
| Rust unit/integration tests | 11 + 5 + 61 + 6 passed, 0 failed |
| `pnpm test` (vitest) | 18 files, 80 tests passed |
| `pnpm test:ui` (Playwright) | 25 passed |
| Real-backup corpus (94 files) | `failures=0`, `parse_errors=3` |
| `cargo fmt --all -- --check` | clean |
| `cargo clippy --workspace --all-targets` | clean, no warnings |
| `pnpm format:check` | **fails on 3 files** (see B4) |

Reviewed by reading: the `flightlens-core` crate (parser, compatibility,
analysis, throttle, export, feedback, session, source, workspace), the Tauri
shell (`main.rs`, `portable.rs`), the Tauri configuration and capabilities, the
React frontend, both release workflows, and the roadmap/TODO/AGENTS documents.

## What is already right

Worth stating, because several of the findings below are small next to it.

- **The desktop security posture is tight.** Strict CSP with no remote origins,
  capabilities limited to `core:default` + `dialog:default`, no filesystem or
  shell plugin, and every path grant originating in Rust from a native dialog,
  a drop event, or a canonicalized entry inside a native-granted workspace root.
  `WorkspaceIndex::path` re-canonicalizes and re-checks root containment on
  every resolve, and symlinks are skipped during discovery.
- **Originals are genuinely never overwritten.** Every write path uses
  `OpenOptions::create_new(true)`.
- **Portable session opening is not a blind grant.** A native dialog lists every
  reference before anything is read, and each backup must match its recorded
  SHA-256 or it is reported rather than opened.
- **Redaction is an explicit key list with a written rationale**, and it covers
  the renamed spellings across 4.2–4.5 rather than only the newest.
- **The "honest about uncertainty" claim holds in the code**, not just the
  README: `derive_defaults` refuses to read back a reset value unless the exact
  release is in the pack's verified list, and says why in a diagnostic.

---

## A. Defects

### A1 — Throttle preview is unavailable on most 4.2/4.3/4.4 patch releases

**Completed.** Added exact 4.2.1–4.2.11, 4.3.1 and 4.4.1–4.4.3 coverage after
compiled upstream C differential verification and CLI/constant checks. Pinned
commits and reproduction commands are in [the throttle evidence](throttle-curve-preview.md).
Missing settings still require the separate omitted-default recovery work.

`crates/flightlens-core/src/throttle.rs:26-41` gates the preview on a literal
release list: `4.2.0`, `4.3.0`, `4.3.2`, `4.4.0`, `4.5.0`–`4.5.5`, plus two
vendor builds. The 4.5 line is covered patch by patch; the older lines are not.
Corpus files on `4.4.2`, `4.4.3` and `4.3.1` therefore report "not verified for
this firmware release or schema pack" even though they match a bundled schema
pack and are official releases.

This is the gate working as designed — the release list is per-release on
purpose — so the work is verification, not a code fix: run the existing upstream
differential fixture generator against the remaining patch tags on the 4.2, 4.3
and 4.4 lines and extend the list with whatever it proves. Consider moving the
list out of the match arm and into a pack field, so extending coverage becomes a
compatibility-data change rather than a core edit.

Impact: `4.4.x` is one of the most widely flashed lines in the wild, and the
Rate Profile tab's second graph is simply absent for it.

### A2 — Audit reference URLs are built from the declared version, so vendor builds get a dead tag

**Completed.** References use the matched schema pack's tag, the UI labels them
as schema references, and unmatched/unknown firmware has no reference. Regression
coverage includes vendor suffixes, plain patch releases, and unsupported or
missing versions.

`crates/flightlens-core/src/analysis.rs:279-298`:

```rust
let version = d.firmware.version.as_deref().unwrap_or("4.5.0");
// ...
reference: format!("{source}{version}/src/main/{file}"),
```

`d.firmware.version` preserves the full declared identity, including suffixes —
`4.5.3.KAACK_V19`, `2025.12.3-alpha.KAACK_V19`. Neither is a Betaflight git tag,
so the URL 404s. The `unwrap_or("4.5.0")` branch is worse in kind: it names a
firmware version the document never declared.

The URL is currently only a `title` tooltip (`src/App.tsx:1224-1227`), so nobody
lands on the 404 by clicking, which is why this is low severity rather than
medium. Fix: build the tag from the matched pack (`pack.version`) and label it as
the certified schema line rather than the file's declared version — that is what
the rule was actually evaluated against. Drop the `"4.5.0"` fallback and omit the
reference when no pack matched, matching the `not_applicable` status the rule
already carries in that case.

### A3 — A feedback report can be unfilable with an error that tells the reporter to do what they already did

**Completed — error guidance.** The rejection now calculates how many characters
to remove from the description's end using the actual encoded URL budget, with
separate guidance when the subject or environment leaves too little room.
Rust regression coverage verifies CJK, Cyrillic, and emoji descriptions.
The alternative clipboard fallback remains deferred in TODO.md.

`crates/flightlens-core/src/feedback.rs:430-434`. `problems()` bounds the
description at `BODY_LIMIT` (2 000 characters). `build_report` then
percent-encodes the assembled body and rejects the result above `URL_LIMIT`
(8 000 bytes) with:

> This report is too long to prefill. Shorten the description to under 2000 characters.

For a description already under 2 000 characters, that instruction is satisfied
and the report still cannot be filed. `encode()` emits three characters per byte
for everything outside the unreserved set, so a Cyrillic or CJK description
expands roughly 9× — around 700 characters is enough to exceed the URL limit with
nothing the reporter can act on.

Fix: derive the message from the actual remaining budget ("shorten the
description by about N characters"), or — better — treat an over-long body the
same way an over-long configuration is already treated: put it on the clipboard
with instructions to paste, instead of refusing.

### A4 — The feedback dialog rebuilds the whole report on every keystroke

**Completed.** Preview generation now waits for 250 ms of inactivity and retains
stale-response and preview/submission matching protections. Playwright coverage
checks typing bursts and asynchronous response ordering.

`src/Feedback.tsx:59-79`. The effect's dependencies include `subject` and
`body`, so each typed character invokes `feedback_report`. With a configuration
attached, that re-runs redaction across the entire document in Rust and ships the
complete redacted text back across IPC — up to the 16 MiB import limit — for
every keypress.

Fix: debounce (~250 ms), or split field validation (cheap, live) from preview
construction (expensive, on blur or on an explicit "Prepare preview" action).
The preview-must-match-submission invariant added in 0.10.x still holds either
way, since the submission rebuilds the report in Rust regardless.

### A5 — `SourceRegistry.files` grows for the life of the process — completed

Implemented canonical-path ID reuse and source cleanup after the last owning
document closes. Ownership tracking preserves reload/session references for older
snapshots even when the newest import closes first. Regression tests cover both
close orders, duplicate content, deleted files, and fresh IDs after reopening.
Follow-up completed 2026-09-15: retained registrations (including failed and
unattempted imports) are capped at 4,096 without evicting queued source IDs.
The native drop queue is capped at 256 and deduplicates queued IDs. Full queues
and registries report actionable errors. Capacity is recovered when the last
owning document closes; failed/unattempted grants otherwise last until restart.

Original finding:

`crates/flightlens-core/src/source.rs:22-40`. `register()` allocates a fresh
`source-N` on every pick, drop, session restore and workspace open, and nothing
ever removes one: `forget_document` prunes `documents` and the saved session, but
not `files`. Registering the same canonical path twice also produces two ids for
one file.

Small in absolute terms, but it is the only unbounded structure in a code base
that carefully bounds everything else (32 documents, 128 MiB, 10 000 entries,
100 000 visits, 64 levels, 256-file batches). Fix: return the existing id when
the canonical path is already registered, and drop the `files` entry in
`forget_document`.

### A6 — `WorkspaceIndex::page` does not snap a caller offset to the page stride — completed

Implemented 2026-09-15: offsets round down to a 50-entry page boundary and
clamp to the last matching page. Existing UI navigation is unchanged. Workspace
regression coverage includes off-stride offsets, `usize::MAX`, and filtered or
empty results.

Original finding:

`crates/flightlens-core/src/workspace.rs:162`:

```rust
let offset = offset.min(matching.len().saturating_sub(1) / 50 * 50);
```

This clamps the ceiling but passes any value below it through unchanged, so
`page(q, 37)` returns a window overlapping the previous page. Latent only —
`WorkspaceExplorer` sends multiples of 50 — but the command is reachable from the
renderer, and every other IPC boundary in this code base normalizes its input
rather than trusting it. Fix: `offset - offset % 50` after the clamp.

---

## B. Tooling and process

### B1 — There is no JavaScript/TypeScript linter

**Completed (2026-09-15).** ESLint checks renderer code, browser/unit tests,
TypeScript tooling and its own configuration with recommended JavaScript and
TypeScript rules plus `rules-of-hooks` and `exhaustive-deps`, both errors.
`pnpm lint` rejects warnings and unused suppressions; it runs in `test.sh`,
branch/PR CI and both release workflows. Compiler-specific React rules are
outside this checkpoint. The documented startup-only effect suppression remains;
its listeners use stable store actions and the repository ref. Other effect
findings now use explicit scalar dependencies and a memoized feedback request,
preserving debounce and stale-response protection. Feedback test globals are typed.

Validation: `./test.sh` passed, including all 80 frontend unit tests and 27
renderer tests. Formatting and diff checks passed; a synthetic invalid component
confirmed both hook rules reject regressions. GitHub execution remains pending.

Correction to the original claim below: hook dependency linting does not detect
excessive request frequency or prescribe the debounce required by A4.

`src/App.tsx:153` carries `// eslint-disable-next-line react-hooks/exhaustive-deps`,
but ESLint is not in `package.json` and never runs. The suppression is inert, and
the `react-hooks` rules that would catch stale-closure and missing-dependency
bugs have never been applied to this frontend. Adding `eslint` +
`eslint-plugin-react-hooks` is a contained change that would flag A4 on the spot.

Recommended as the highest value-per-effort item in this document.

### B2 — `cargo clippy` runs nowhere

**Completed.** Warning-free workspace Clippy checks now run in `test.sh`, both
release workflows, and the new branch/PR workflow. CI explicitly installs Clippy
and rustfmt; both release workflows also check Rust formatting.

It passes clean today with default lints (verified in this review), which makes
now the cheapest possible moment to wire
`cargo clippy --workspace --all-targets -- -D warnings` into `test.sh` and both
release workflows. Adding it later, after drift, costs a cleanup pass first.

### B3 — CI only runs on `v*` tags — reinforces the existing TODO item

**Completed — configuration; GitHub execution pending.** The new Checks workflow
runs on branch pushes and pull requests on Windows and macOS, covering Rust
formatting, Clippy, core tests, generated bindings, TypeScript, Vitest and
Playwright. Release jobs run the same checks before building installers. The
private corpus stays local. Broader security scanning and badges remain in TODO.md.

Already tracked under "Add free security checks, release gating and status
badges to CI", and this review supports raising its priority. Two points worth
adding to that item:

- The release workflows are currently the *only* CI. A commit that breaks the
  build on `main` is discovered when a release is attempted.
- Neither workflow runs `cargo fmt --check` nor the corpus check, so the
  release-gate checks are a strict subset of `./test.sh`.

### B4 — `pnpm format:check` now fails on three files, not two — completed

Implemented 2026-09-15: ran the existing Prettier configuration on all three
affected files. The full formatting check now passes. Removed the resolved TODO; no commit has been created.

Original finding:

TODO records `src/App.tsx` and `tests/inspector.spec.ts`;
`tests/throttle-comparison.spec.ts` has since joined them. The drift is the
argument for resolving it: a check that is always red stops being read. Recommend
the "reformat in a commit of its own" option already written in TODO, taken soon
rather than later.

---

## C. Architecture and maintainability

### C1 — `src/App.tsx` is 1 733 lines

**Implemented (2026-09-15).** App now contains document/session orchestration
(under 600 lines). `Inspector.tsx` owns inspection loading, profile selection,
and source navigation (under 300 lines). Rates, PID, Ports, Modes, Raw, Audit,
Filters, OSD and Export each have a separate component; parameter/derived tables
and backup notices are shared components. Conditional tab mounting and existing
state ownership are preserved. Profile helper tests now import their own module.
Verified with TypeScript, ESLint, 80 frontend unit tests, 27 browser tests,
both screenshot baselines, Prettier and diff checks.

It holds `App`, `Inspector`, `Osd`, `Filters`, `Export`, `ParameterTable`,
`DerivedTable`, `Empty`, `BackupInstructions` and `MissingSettingsNotice`. The
comparison half of the app is already one file per concern — `Comparison.tsx`,
`ParameterComparison.tsx`, `PortComparison.tsx` and so on, none over 300 lines —
so the inspector half is the outlier, not the convention. Splitting `Inspector`
and its tab bodies out would follow a pattern the repository already has, and
would incidentally shrink the prettier diff in B4.

### C2 — Every document is held twice and crosses IPC in full

**Completed (2026-09-16).** Every paste/file/workspace/session import now returns
an ArtifactView with compact DocumentView inspection fields. The backend moves
the original document into shared storage without an initial clone. Raw requests
at most 500 original lines per page, handles errors/retry, ignores stale replies
on navigation/reload/close, and scrolls source jumps after loading. Native tests
verify retained source allocation and exact raw paging; browser tests use actual
Rust compact DTOs, including delayed replies and restored sessions. The synthetic
16 MiB comment case now transfers 20,928 bytes instead of 22,335,176 bytes.
Validation: `./test.sh` passes (94 core tests, 84 frontend tests, 28 browser
tests, bindings, lint/type checks, Clippy and local corpus coverage); all 14
native tests, both screenshot checks, the production build and formatting/diff
checks also pass.
Packaged desktop memory/latency remains unmeasured; the 128 MiB input limit is
still not a total-memory limit. See [measurements](document-ipc-cost.md).

Earlier implementation checkpoints follow for context:

**Assessment completed (2026-09-15); implementation remains open.**
[Reproducible synthetic measurements and implementation boundary](document-ipc-cost.md)
confirm payload/heap amplification. The original claim below that only Raw reads
syntax is incorrect: comparisons replay reset/malformed command evidence and
recover source declarations, and Inspector uses syntax for dump-all detection.
The 128 MiB bound counts input text, not heap memory. A smaller DTO must preserve
that evidence before raw paging can replace full syntax transfers.

**Backend snapshot slice implemented (2026-09-15).** Repository lookups now
clone an Arc handle instead of the full ConfigDocument. Analysis, export,
feedback and session validation share immutable snapshots without retaining
the repository lock. Closing/replacing an entry preserves in-flight snapshots;
the last handle releases them. The initial import clone and full IPC payload
remain until the DTO/raw-page boundary is implemented. All 11 native tests,
workspace Clippy with warnings denied, Rust formatting and diff checks pass.

**Source-evidence projection implemented (2026-09-16).** The core now exposes
line count, dump-all status and compact ordered comparison evidence, with
TypeScript bindings. It preserves reset/malformed commands and source locations;
only comments, blanks and settings absent from final parameter references are
removed. Unknown-scope declarations remain intact. Five new tests and all core
tests pass, along with workspace Clippy, bindings/type checks and formatting.
The synthetic 16 MiB comment case reduces source evidence from 22,320,671 to
6,415 JSON bytes; this excludes the rest of the document. Runtime IPC is still
unchanged. Frontend equivalence checks, the document DTO and raw paging remain
open; see the updated measurement document.

**Frontend comparison boundary implemented (2026-09-16).** Comparisons and source
cells now accept the compact DocumentView without requiring full syntax. A
cross-language test runs the Rust parser/projection on 14 synthetic documents
and checks all 196 pairs against full-document comparison results, plus source
references and three-document parameter comparisons. The 84 frontend tests,
27 UI tests, type/lint checks and workspace Clippy pass. Runtime imports and Raw
still use full documents; returning the view over IPC and raw paging remain open.

**Backend raw paging implemented (2026-09-16).** The `raw_page` command returns
at most 500 original syntax lines from an immutable snapshot, rejects invalid
page sizes and closed documents, and returns an empty page beyond EOF. Tests
reassemble a multi-page Unicode/CRLF input exactly, including its final line
without a newline, and exercise boundary/overflow-sized offsets. All 13 native
tests pass. The renderer still uses full documents; compact import/session DTOs
and asynchronous Raw-tab integration remain open.

**Generated document-view contract implemented (2026-09-16).** Rust now projects
`DocumentView` directly and generates its TypeScript binding. A serialization
regression checks every non-source field against the full document, checks the
absence of full syntax and round-trips the DTO. The 196-pair frontend comparison
suite now consumes serialized Rust views directly, removing its handwritten
JavaScript projection. Core and frontend tests, type/lint checks, bindings,
formatting and workspace Clippy pass. Runtime imports still return full artifacts;
the import/session switch and asynchronous Raw-tab integration remain open.

`AppState.documents` keeps the parsed `ConfigDocument` in Rust (bounded at 32
documents / 128 MiB), and `App.tsx`'s `repository` ref keeps the deserialized
`Artifact` — including the complete `syntax` array with every raw line — in the
webview. A 16 MiB dump is serialized in its entirety across IPC on open, even
though the Raw tab paginates at 500 lines and no other tab reads `syntax`
directly.

Consider returning a document summary from `open_source` and adding a
`raw_page(config_id, offset, count)` command for the Raw tab and the source-line
jump. This is a design note, not a reported problem — no measurement in this
review showed it hurting.

### C3 — `repository` as a ref plus a revision counter

**Implemented (2026-09-15).** Artifacts now live in the in-memory Zustand
workspace store. Adding/replacing or closing a document publishes its artifact
and sidebar metadata together; App subscribes directly without a ref or revision
counter. Portable-session profile restoration reads the current store after
imports. Persistence still writes only navigation preferences, never artifacts.
Store tests cover replacement notifications, immutable prior snapshots, atomic
close, and preference-only persistence. TypeScript, lint, all 81 unit tests,
all 27 browser tests (including portable sessions), formatting and diff checks pass.

`src/App.tsx:49-51` and the bare `void revision;` at line 156 work around React's
render model rather than using it. Zustand is already a dependency and already
holds the document metadata; the artifact map could live beside it. Low priority,
but it is the one place in the frontend where the mechanism is visible in the
code.

---

## D. Product and coverage

### D1 — PID values are empty in almost every corpus backup

**Completed (2026-09-16), with a deliberately bounded recovery table.**
Official Betaflight 4.5.0–4.5.5 now recover the ten build-invariant gains:
P/I/F on roll, pitch and yaw, plus yaw D. A valid final reset and a profile
within the certified bounds are required. Explicit values, including invalid
ones, block recovery; malformed reset/profile/assignment commands and unmodeled
`simplified_tuning` operations leave omissions unknown until a later valid reset.
`disable` alone preserves gains. Vendor, prerelease and uncertified versions
remain excluded. Roll/pitch D and D-min/D-max are intentionally not certified.

The PID inspector labels recovered values and their origin without presenting a
source link. Two-/three-way comparisons consume the same derived values, and
export continues to include only explicit settings. Corpus coverage counts now
include recovered gains without treating ten gains as a complete twelve-gain
profile. Parser, renderer, comparison and export-exclusion regressions cover
these boundaries.

[PID evidence](pid-default-recovery.md) records the source/build contract,
immutable archive checks across all six releases, and execution of the full
PID-profile reset/dispatcher with D-min enabled/disabled and default tuning at
O0/O2/Ofast under sanitizers. The selected ten fields avoid feature-dependent D
and non-default floating-point tuning. This completes D1's first supported
recovery scope; expanding firmware/build coverage is tracked separately in TODO.
`./test.sh`, 14 native tests, both screenshot checks, production build and
formatting checks passed.
The 97-backup corpus has zero failures: complete PID profiles increased from
23 to 47, and profiles with known gains from 104 to 171.

Original finding follows for context:

The corpus run reports `pid=0/4` and `WARN PID coverage: no known PID values in
any discovered profile` on the large majority of files, with only
`complete_pid_profiles=23` out of `pid_profiles_with_values=104`.

The cause is deliberate and documented: `Defaults` certifies only
`PG_CONTROL_RATE_PROFILES`, and `derive_defaults`
(`crates/flightlens-core/src/parser.rs:50`) iterates rate profiles alone. A
`diff`-based backup omits unchanged PID values, and FlightLens correctly refuses
to invent them.

Extending the verified reset table to `PG_PID_PROFILE`, on the same evidence
standard already used for rate profiles, is the single largest inspection
coverage win available and is not currently in TODO. It would light up the PID
tab for the diff backups that make up most real-world files. The existing
`tools/build_compatibility.py` pipeline and the "proven identical at every patch
release on the line" bar apply unchanged; the work is the verification pass, and
the comment on `Defaults` already says why it has not been done.

### D2 — Vendor builds render an empty Rates tab

**Completed (2026-09-16).** Chose source verification of exact vendor builds.
`4.5.3.KAACK_V19` and `2025.12.3-alpha.KAACK_V19` now recover omitted
rate-profile values after a valid reset, using independently verified pinned
vendor source. Recovery names the exact vendor version, preserves explicit zero
and invalid declarations, respects profile/reset boundaries and never exports
recovered settings. Ambiguous reset, rate-profile or assignment commands prevent
recovery until a new valid reset. Other vendor identities and vendor PID defaults
remain unverified; throttle previews still require explicit inputs.

The developer-only verifier checks reset functions and parameter-group machinery,
CLI mappings/lookups, constants, version identities and absence of target callback
implementations, recording immutable source hashes in the offline table. See
[vendor recovery evidence](vendor-rate-defaults.md).

Validation: the source verifier, `./test.sh` (101 core tests, 86 frontend tests,
31 browser tests, formatting, Clippy, bindings, lint/typecheck and corpus), 14
native tests, both screenshot checks, format check and production build passed. On the same 97-backup
corpus, complete selected rate profiles increased **77 → 89**, complete rate
profiles **349 → 397**, with zero failures and the same three existing parse
errors. All 52 observed rate profiles across the 13 matching vendor backups are
complete. Backup inputs remain unchanged.

Original finding follows for context:


Every `4.5.3.KAACK_V19` file in the corpus reports `rates=0/4 derived=0`, because
`defaults_certified()` requires an exact match in the pack's verified-release
list and a vendor suffix can never match. The reasoning is sound and the app
explains itself through `derived_note` and an `info` diagnostic, so this is not a
defect — but it is worth an explicit product decision, because it is the state
the most recent backups in the local corpus are in.

Two directions, both compatible with the project's stated principles:

- An opt-in, per-document "read back 4.5 reset values for this unverified build"
  control, with the derived values labelled as unverified everywhere they appear
  and excluded from export exactly as derived values already are.
- A verification path for specific vendor builds, the way
  `2025.12.3-alpha.KAACK_V19` is already enumerated in the throttle gate.

Doing neither is also a defensible answer; what is worth avoiding is leaving it
implicit.

### D3 — `mode_name` covers 19 box IDs; the rest render as "Mode ID 37"

**Implemented (2026-09-15).** Mode labels now use exact-release tables verified
against pinned upstream `msp_box.c` for 30 official releases across all five
supported lines and both enumerated KAACK builds. Removed, absent and unknown
IDs remain numeric; unverified versions retain the previous fallback labels.
The generator records source URLs and hashes and supports `--check`.
See [mode-name evidence](mode-names.md).

`crates/flightlens-core/src/parser.rs:673`. Several modes pilots actually
assign fall outside the table and show as a bare numeric id in the Modes tab and
in the mode comparison. Betaflight's `boxId_e` values are permanent by design,
so filling out the table is low-risk — but it still needs the same per-line
source verification the rest of this project applies, since which ids exist
varies by firmware line even when their meanings do not.

### D4 — Save dialog, then a refusal to overwrite, is a confusing two-step

**Implemented (2026-09-15); packaged OS dialog validation remains pending.**
Snippet and session saves share a native save helper that suggests an unused
filename in Documents and retries `AlreadyExists` with an explanatory dialog
title, the selected directory and an unused numbered filename. Atomic
`create_new` still protects existing files, including races after the suggestion.
Other creation failures report a location error rather than falsely claiming a
collision. Cancellation returns normally. Session manifests are rebuilt for
each selected destination so relative references remain correct after retries.
Rust regression coverage verifies collision preservation, changed directories
and cancellation; packaged Windows/macOS dialog checks are tracked in TODO.md.

Original finding:

`src-tauri/src/main.rs` (`save_snippet`) and `src-tauri/src/portable.rs`
(`save_portable_session`) both open a native save dialog and then refuse with
`create_new(true)`. A user who selects an existing file and confirms the
operating system's own "replace?" prompt is then told "Choose a new filename.
Existing files are never overwritten."

The refusal is right; the sequencing is not. Suggest pre-seeding a
non-colliding default filename, and on `ErrorKind::AlreadyExists` reopening the
dialog with the explanation attached rather than dropping the user back to the
main window with an error banner.

---

## E. Accessibility

### E1 — Modals do not trap or restore focus

**Completed — automated verification; native validation pending.** Both dialogs
use a shared native dialog wrapper with initial focus, Tab/Shift+Tab containment,
Escape dismissal, and focus restoration. The paste shortcut cannot stack a
second modal. Playwright checks both dialogs; packaged Windows/macOS validation
remains tracked in TODO.md.

The paste dialog (`src/App.tsx:449+`) and the feedback dialog both declare
`role="dialog"` and `aria-modal="true"`, but Tab moves out of the dialog into the
page behind it, and closing the dialog does not return focus to the control that
opened it. Escape closes the paste dialog only, through the global keydown
handler; the feedback dialog has no Escape path.

### E2 — Plot values are mouse-only — completed

Implemented 2026-09-15: each plot with samples has a native expandable
“Sample values” section containing a table per curve with explicit units.
Tables use each curve’s own backend samples, rounded to four decimal places;
stick inputs are displayed as percentages. The bounded scrolling region is
keyboard-focusable, and unknown curves retain their reasons without empty tables.

Original finding:

`src/Plots.tsx:39-56` drives the hover readout from `onMouseMove` /
`onMouseLeave`. The SVG's `aria-label` describes the chart well, but the per-point
values in the legend are unreachable by keyboard and by touch. Suggest pointer
events plus a focusable plot with arrow-key scrubbing, or — cheaper and arguably
more useful for a bench tool — a value table behind a `<details>` element.

### E3 — `theme-color` is fixed dark — dismissed (already handled)

Verified 2026-09-15: `src/stores/theme.ts::applyTheme` already reads the active
`--bg-app` CSS token and updates the meta element. `src/main.tsx` calls it at
startup, and `setTheme` calls it for subsequent changes, including restored
session preferences. These paths were present at the review baseline; the
static HTML value is only a fallback. The existing Playwright theme-switch test
checks that the meta content changes when the theme changes. No code fix needed.

Original finding:

`index.html:6` declares `#10181c` regardless of the selected theme. Trivial;
listed only because the light theme is otherwise carried through consistently.

---

## Suggested order

1. **B1 — completed:** JavaScript/TypeScript and React hook lint gates.
2. **B2 — completed:** Clippy in `test.sh` and CI.
3. **A4 — completed:** debounce the feedback preview.
4. **A3 — completed:** the unfilable-report error message.
5. **A2 — completed:** audit reference tag.
6. **B4** completed: the existing Prettier check passes for all included files.
7. **D1** `PG_PID_PROFILE` reset verification — the largest coverage win, and the
   largest piece of work here.
8. **A1 — completed:** throttle coverage for the 4.2/4.3/4.4 patch releases.
9. **C1**, **D4** and **E1** are implemented;
   packaged Windows/macOS validation remains.
10. **D2 — completed:** exact vendor rate-default verification. **D3** is implemented.

At review time, items B3 and the vendor-build question in D2 overlapped with work
already tracked in [TODO.md](../TODO.md); the rest were new.
