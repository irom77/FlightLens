{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local window events; all source access is authorized in Rust.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
